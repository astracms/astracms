import PageClient from "./page-client";

export const metadata = {
    title: "Workflow Builder",
    description: "Build and configure your AI workflow",
};

function Page() {
    return <PageClient />;
}

export default Page;
